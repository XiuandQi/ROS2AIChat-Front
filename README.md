# ROS2 AI问答助手 - Vue版本

这是原始HTML/JS版本转换为Vue 3项目的版本，使用Vite作为构建工具。

## 功能特性

- 🤖 ROS2 AI智能问答
- 🌐 基于WebSocket的实时通信
- 💬 优雅的聊天界面
- ⚡ 打字机效果
- 🔄 自动重连机制
- 📱 响应式设计

## 技术栈

- Vue 3 (Composition API)
- Vite
- ROSLIB.js
- CSS3 动画

## 安装依赖

```bash
cd vue
npm install
```

## 开发模式

```bash
npm run dev
```

应用将在 `http://localhost:3000` 启动

## 构建生产版本

```bash
npm run build
```

构建文件将输出到 `dist` 目录

## 预览构建版本

```bash
npm run preview
```

## 使用说明

1. 确保ROS2系统运行并启动rosbridge：
   ```bash
   ros2 launch rosbridge_server rosbridge_websocket_launch.xml
   ```

2. 打开应用，点击右上角设置按钮

3. 配置WebSocket地址（默认：ws://localhost:9090）

4. 点击"连接"按钮建立连接

5. 在输入框中输入问题，开始与AI助手对话

## 项目结构

```
vue/
├── src/
│   ├── components/          # Vue组件
│   │   ├── AppHeader.vue    # 头部组件
│   │   ├── ChatMessages.vue # 消息显示组件
│   │   └── ChatInput.vue    # 输入组件
│   ├── composables/         # 组合式函数
│   │   └── useRosConnection.js # ROS连接逻辑
│   ├── App.vue             # 根组件
│   ├── main.js            # 入口文件
│   └── style.css          # 全局样式
├── public/
│   └── roslib.min.js      # ROS库文件
├── package.json
├── vite.config.js
└── index.html
```

## 与原版差异

- 使用Vue 3的响应式系统管理状态
- 组件化架构，代码更易维护
- 使用Composition API，逻辑复用性更强
- Vite构建工具，开发体验更好
- 保持了所有原有功能和视觉效果